import React from 'react';

const PricingCTA = () => (
  <div className="container">
    <h3 className="cta-subtext text-dark">One Price</h3>
  </div>
);
// {CTAText, subtext, handleEmail, classes}) -> props we expect to pass in
export default PricingCTA;
